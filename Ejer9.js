// Declaramos dos variables
let x = 8;
let y = 15;

// Intercambiamos valores usando una variable auxiliar
let aux = x;
x = y;
y = aux;

// Mostramos los resultados
console.log("x: " + x);
console.log("y: " + y);
